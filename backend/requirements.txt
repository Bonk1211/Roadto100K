# AWS SDK (included in Lambda runtime, pinned for local dev)
boto3>=1.34.0,<2.0
botocore>=1.34.0,<2.0

# HTTP client for Alibaba EAS calls
requests>=2.31.0,<3.0

# Utilities
python-dateutil>=2.8.0,<3.0
